name: ROMM
slug: romm
version: "1.0.1"
arch:
  - amd64

startup: services
boot: auto
init: false

environment:
  ROMM_AUTH_SECRET_KEY: "super_long_random_secret_please_change"

ports:
  8080/tcp: 8080

map:
  - share:rw
  - config:rw

webui: http://[HOST]:[PORT:8080]
